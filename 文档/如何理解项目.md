这个项目最适合先抓两条主线：

1、运行时主线：协程 → Scheduler → IOManager/epoll → Hook → Socket → TCP/HTTP Server。
这条线回答“框架为什么能用少量线程处理大量网络连接”。

2、HTTP 工程化主线：连接池 → 请求选项/超时 → HttpClient/重试 → 负载均衡 → 限流 → 熔断。

每次只选一个场景，按这套四步走：
先写一句话：它解决什么问题？
找入口：谁调用它、它调用谁？
画出最多 5 个节点的数据/控制流。
跑对应测试或自己改一个小边界，再回头读实现。