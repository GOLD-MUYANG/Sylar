# websocket
http是单向的连接，只能客户端给服务器发请求，服务器不能主动向客户端推送消息
websocket就是为了解决这个问题的

# ssl
在普通 TCP Socket 的基础上，加一层 OpenSSL，让原本明文的 send/recv 变成加密传输。

```
普通 Socket:
应用数据 -> send() -> TCP -> 网络

SSLSocket:
应用数据 -> SSL_write() -> TLS 加密 -> TCP -> 网络
网络 -> TCP -> TLS 解密 -> SSL_read() -> 应用数据
```

sylar项目里这是一种老式 OpenSSL 初始化写法。

在 OpenSSL 1.1.0 之后，很多初始化已经自动完成了，这些函数很多已经不再必要。但 Sylar 这种老项目里这么写很常见。