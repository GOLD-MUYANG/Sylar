# 2026-06-29 真实 Provider 演示网关

## 目标

根据 `文档/真实Provider演示网关任务书.md` 推进 D1-D7：新增真实 Provider 运行时配置、接入 servlet、补 trace/status/demo 页面和手工 opt-in 演示脚本。默认测试和脚本不得依赖真实 API Key、外网或计费账户。

## 排查与实现命令

1. `rg --files | rg '(^modules/ai_gateway|^examples/ai_gateway|^scripts/smoke_ai_gateway|^tests/test_ai_gateway|CMakeLists.txt$|ai_gateway)'`
   - 原因：确认 AI Gateway 相关源码、测试、脚本和 CMake 入口。
   - 结果：确认真实 Provider 底层已有 `real_provider_gateway.*`、`ai_provider_adapter.*`、`real_provider_smoke.*`，但缺少运行时配置接入层。

2. `sed -n '1,260p' 文档/真实Provider演示网关任务书.md`
   - 原因：按任务书 slice 确认实现边界。
   - 结果：D1-D7 都是待实现，且要求真实 Provider 默认关闭、Key 只来自环境变量、页面展示状态和 timeline。

3. `cmake -S . -B build`
   - 原因：新增 `test_ai_gateway_real_runtime` 后刷新 CMake target。
   - 结果：RED 阶段失败在缺少 `modules/ai_gateway/real_provider_runtime.cc`，符合新增运行时层预期。

4. `cmake --build build --target test_ai_gateway_real_runtime`
   - 原因：验证 D1-D3 的运行时配置、fake transport、trace/status 脱敏。
   - 结果：通过。

5. `cmake --build build --target test_ai_gateway_servlet`
   - 原因：验证 servlet 新增真实 Provider 回调，不再把真实响应当 mock 响应解析。
   - 结果：先因构造函数缺失失败，补 `CompatibleUpstreamPost` 后通过。

6. `cmake --build build --target test_ai_gateway_status`
   - 原因：验证 `/internal/status` 可挂载真实 Provider 状态段。
   - 结果：先因三参数构造函数缺失失败，补真实状态回调后通过。

7. `cmake --build build --target ai_gateway_module mock_model_provider test_ai_gateway_real_runtime test_ai_gateway_servlet test_ai_gateway_status`
   - 原因：验证模块、契约兼容 mock provider 和新增测试目标都能构建。
   - 结果：通过。

8. `./bin/test_ai_gateway_real_runtime`
   - 原因：验证 env-only key、真实 provider fake transport、trace/status 脱敏。
   - 结果：通过。

9. `./bin/test_ai_gateway_servlet`
   - 原因：验证 mock 路径保持兼容，真实 Provider servlet 分支能返回兼容响应和 trace header。
   - 结果：通过。

10. `./bin/test_ai_gateway_status`
    - 原因：验证 mock status 与 real provider status 共存。
    - 结果：通过。

11. `./scripts/demo_ai_gateway_real_provider.sh`
    - 原因：验证真实页面演示脚本默认拒绝执行，不读取 Key、不触网。
    - 结果：未设置 `SYLAR_AI_GATEWAY_REAL_DEMO=1` 时拒绝执行，符合默认关闭边界。

12. `cmake --build build`
    - 原因：完整构建源码、模块、默认测试目标和 `bin_sylar`。
    - 结果：通过。

13. `ctest --test-dir build -R ai_gateway --output-on-failure`
    - 原因：验证 AI gateway 相关 CTest。
    - 结果：沙箱内 `ai_gateway_load_balance` 因 `socket(2, 1, 0) errno=1 Operation not permitted` 失败；这是本地 socket/bind 权限限制。

14. `ctest --test-dir build -R ai_gateway --output-on-failure`（非沙箱权限）
    - 原因：排除沙箱 socket 权限影响，验证同一组 AI gateway 测试。
    - 结果：8/8 通过。

15. `bash -n scripts/demo_ai_gateway_real_provider.sh`
    - 原因：检查新增真实页面演示脚本语法。
    - 结果：通过。

16. `bash -n scripts/smoke_ai_gateway_real_provider.sh`
    - 原因：确认既有真实 smoke 脚本语法未受影响。
    - 结果：通过。

17. `git diff --check`
    - 原因：检查补丁中是否存在空白错误。
    - 结果：通过。

18. `./bin/test_ai_gateway_real_provider_smoke`
    - 原因：验证真实 Provider smoke 的默认跳过、缺 Key 不触网和 fake transport 单次尝试离线路径。
    - 结果：通过。

## 边界说明

- 本次没有真实 API Key，因此没有验证真实 Provider live 成功返回；D7 只完成手工入口、默认拒绝和缺 Key 不触网边界。
- `scripts/demo_ai_gateway_real_provider.sh` 生成临时配置，不修改仓库内 yml；只有显式 opt-in 并设置本机 Key 后才会启动真实 Provider 页面演示。
