# 2026-06-19 完成第五步负载均衡后半部分

## 排查和验证命令

1. `sed -n '1,240p' 文档/接下来要做什么.md`
   - 目的：确认第五步前后文，判断本次范围是多实例负载均衡后半部分。

2. `sed -n '241,520p' 文档/接下来要做什么.md`
   - 目的：确认第五步剩余项为 WeightedRoundRobin、LeastConnection 和健康检查。

3. `rg -n "class WorkerGroup|WorkerGroup|FiberSemaphore|HttpClient|HttpRetryOptions|doRequest|request\\(" sylar tests CMakeLists.txt`
   - 目的：确认现有 Worker、HTTP Client、负载均衡客户端和测试的代码位置，避免改错边界。

4. `sed -n '1,260p' sylar/http/http_load_balance_client.h`
   - 目的：查看 Endpoint、策略枚举和客户端接口现状。

5. `sed -n '1,320p' sylar/http/http_load_balance_client.cc`
   - 目的：查看现有 RoundRobin、Random 和请求转发实现，确定新增策略的插入点。

6. `sed -n '1,460p' tests/test_http_load_balance_client.cc`
   - 目的：查看现有本地 HTTP 测试工具和测试风格，按同一文件边界补充负载均衡测试。

7. `cmake --build build --target test_http_load_balance_client`
   - 目的：先运行新增测试的红灯检查，确认失败点是缺少 WEIGHTED_ROUND_ROBIN、LEAST_CONNECTION 和 checkHealth。

8. `./bin/test_http_load_balance_client`
   - 目的：运行新增负载均衡测试；普通沙箱下 socket 被拒绝，作为环境限制记录。

9. `./bin/test_http_load_balance_client`
   - 目的：在允许本地 127.0.0.1 socket 后重跑测试，验证 RoundRobin、Random、WeightedRoundRobin、LeastConnection 和健康检查行为。

10. `clang-format -i sylar/http/http_load_balance_client.h sylar/http/http_load_balance_client.cc tests/test_http_load_balance_client.cc`
    - 目的：尝试按项目格式化 C++ 文件；当前环境没有 clang-format，已改为手动检查明显长行和风格。

11. `git diff -- sylar/http/http_load_balance_client.h sylar/http/http_load_balance_client.cc tests/test_http_load_balance_client.cc 文档/接下来要做什么.md`
    - 目的：复查本次源文件、测试和文档改动范围，确认没有碰到无关文件。
