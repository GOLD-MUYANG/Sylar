# 2026-06-27 核实真实提供商网关结构影响

## 背景

用户指出 `文档/真实提供商网关.md` 的实现切口可能存在结构性返工风险，需要先判断这些问题是否确实影响当前项目，再修改任务书。

## 排查命令与原因

1. `rg -n "AI gateway|真实提供商|sylar-ai-gateway|AI模型调用网关|real provider|真实 Provider" /home/muyang/.codex/memories/MEMORY.md`
   - 原因：先确认 AI 网关既有边界，避免把本地 Mock 闭环和真实 Provider 后续项混在一起。

2. `sed -n '1,260p' /home/muyang/.codex/memories/skills/sylar-ai-gateway-workflow/SKILL.md`
   - 原因：读取仓库 AI 网关工作流，确认需要先读文档、再对照实现边界，且真实 Provider 风险应留在 `文档/真实提供商网关.md`。

3. `rg -n "class HttpClient|class HttpConnection|HttpLoadBalanceClient|HttpRequestOptions|HttpResult|sendRequest|doRequest|request\\(|max_retry|retry|timeout|connect_timeout|read_timeout|write_timeout|SSL|ssl|https|TLS|SNI|hostname|certificate|partial|send\\(|recv\\(" sylar/http modules/ai_gateway tests CMakeLists.txt`
   - 原因：快速定位 HTTP 客户端、负载均衡、TLS、重试、超时和 AI 网关调用链相关文件。

4. `sed -n '1,360p' sylar/http/http_client.h`
   - 原因：查看 `HttpRetryOptions`、`HttpClient` 对外接口，确认总尝试预算是否已是共享上下文。

5. `sed -n '1,260p' sylar/http/http_load_balance_client.h`
   - 原因：查看 `HttpLoadBalanceClient` 的职责和接口，确认它当前只接收统一 path、headers、body，不支持每个 Provider 候选独立 adapter。

6. `sed -n '247,410p' sylar/http/http_load_balance_client.cc`
   - 原因：查看 endpoint 选择、limiter、breaker、retry 和 `max_total_attempts` 的实际控制流。

7. `sed -n '1,460p' sylar/http/http_connection.cc`
   - 原因：查看 connect/send/recv 的错误返回和超时处理，确认是否能区分发送前失败、partial write、写完后读超时。

8. `sed -n '300,430p' sylar/http/http_client.cc`
   - 原因：查看 `NormalizeResult()` 和 `ShouldRetry()`，确认当前错误归一化会合并发送错误、接收超时和所有 HTTP 4xx/5xx。

9. `sed -n '340,760p' sylar/socket.cc`
   - 原因：查看 `SSLSocket` 的 TLS 客户端实现，确认已有默认 CA、SNI 和 hostname verification 基础，但还没有 Provider 配置级闭环。

10. `sed -n '1,140p' modules/ai_gateway/ai_gateway_upstream.cc`
    - 原因：查看 AI 网关如何从 provider 配置创建 endpoint，确认当前只把 URL 拆成 host/port/ssl/weight。

11. `sed -n '230,310p' modules/ai_gateway/ai_gateway_module.cc`
    - 原因：查看当前 Mock 网关如何注入上游 POST，确认现阶段直接对所有 provider 使用同一路径、body 和 `retry_non_idempotent=true`。

12. `sed -n '1,260p' 文档/真实提供商网关.md`
    - 原因：读取待修改任务书，逐项对照用户指出的结构问题。

13. `sed -n '1,220p' sylar/stream.cc` 与 `sed -n '1,220p' sylar/socket_stream.cc`
    - 原因：查看 `writeFixSize()` 和 socket write 的行为，确认当前返回值没有向上暴露已写字节数，无法安全判断 partial write 后是否已提交。

14. `sed -n '1,220p' sylar/http/http_request_options.h`
    - 原因：确认当前请求选项只有阶段超时和 total timeout，没有共享预算对象。

15. `sed -n '1,240p' sylar/http/http_concurrency_limiter.h && sed -n '1,220p' sylar/http/http_circuit_breaker.h`
    - 原因：确认 limiter/breaker 当前职责边界，避免把真实 Provider adapter 逻辑塞进治理状态机。

## 结论

用户提出的五个结构风险均存在。TLS 不是完全空白，但现有实现还不足以作为真实 Provider R0 直接验收；任务书已改为先补齐 TLS 配置闭环、HTTP 尝试阶段元数据、provider-aware selector/executor、共享预算对象和细化错误分类。
