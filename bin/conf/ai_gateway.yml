ai_gateway:
  # 默认关闭，避免普通框架启动自动依赖本地 Mock Provider。
  enabled: false
  server_name: ai-gateway
  request_timeout_ms: 800
  load_balance: ROUND_ROBIN
  providers:
    - name: mock-a
      url: http://127.0.0.1:19001
      weight: 1
    - name: mock-b
      url: http://127.0.0.1:19002
      weight: 1
